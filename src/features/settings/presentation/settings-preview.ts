/** Placeholder values for settings UI preview — not wired to live data yet. */
export const SETTINGS_PREVIEW = {
  storeName: "Pawlio Pet Store",
  email: "vendor@pawlio.example",
  statusKey: "active" as const,
  commissionRatePercent: 10,
  productCount: 24,
  openOrders: 3,
  netEarningsPiastres: 124_000,
} as const;
